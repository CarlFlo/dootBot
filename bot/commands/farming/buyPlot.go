package farming

// printFarm button component is turned off for now

func BuyFarmPlotInteraction(discordID string, response *string) bool {

	*response = "You can't buy a plot yet!"

	return false
}
